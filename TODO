- Ability for User X to add (and automatically claim) an item for User Y, but User Y can't see it
  - views (hide from receiver)
  - edit
  - delete

- Better/more styling
- Better/more content on homepage?
- README file

- Unfortunately, time constraints means specs will be ignored
  - Make fixtures/factories better, to avoid hardcoding user password
  - Fix ItemsControllerSpec
  - Spec out everything else
    - list must have user_id
    - item must have title and wantlist_id
    - integration spec for creating item
    - show user
- AJAXify some actions (also learn some CoffeeScript)
